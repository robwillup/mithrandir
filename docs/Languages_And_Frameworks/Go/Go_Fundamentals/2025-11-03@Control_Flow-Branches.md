# Control Flow: Branches

```Go
if test-expression { ... }      // The test expression evaluates to a Boolean

if initializer; test { ... }

// how to read input from a user in Go

fmt.Println("What's your name?")
var name string
fmt.Scanln(&option)

// Switch Statements

switch test expression {
    case expression:
        statements
    case expression2, expression3:
        statements
    default:
        statements
}
```



